# PHONE AIM 프로토타입

화면 네 모서리의 색 기준점을 휴대폰 카메라로 찾아, 카메라 정중앙이 화면에서 가리키는 좌표를 계산하는 로컬 웹 프로토타입입니다.

## 실행

Windows PowerShell에서 이 폴더로 이동한 뒤, Node.js 18 이상으로 실행합니다.

```powershell
node server.js
```

컴퓨터 화면에서 `http://localhost:4173`을 열면 휴대폰 접속 주소와 QR 코드가 표시됩니다. 컴퓨터와 휴대폰은 같은 Wi-Fi에 연결되어 있어야 합니다.

## 현재 프로토타입에서 가능한 것

- 화면 네 모서리의 색 기준점 표시
- 휴대폰 카메라 영상에서 네 기준점 검출
- 원근 변환으로 카메라 중심의 화면 좌표 계산
- 휴대폰 좌표를 로컬 서버를 통해 화면으로 전송
- 카메라를 쓸 수 없는 환경을 위한 사진 측정과 터치 시뮬레이션

## 중요한 제한

일반 모바일 브라우저는 보안 정책 때문에 로컬 HTTP 주소에서 실시간 카메라 사용을 막을 수 있습니다. 이 경우 사진 측정과 시뮬레이션은 사용할 수 있지만, 실시간 카메라 추적은 HTTPS 환경이 필요합니다. 축제용 다음 단계에서는 학교 내부망용 신뢰 인증서 또는 비공개 HTTPS 테스트 주소를 붙이는 방식이 적합합니다.

GitHub에는 소스만 등록되어 있습니다. 실행은 저장소 폴더에서 `node server.js`로 시작하세요. GitHub Pages와 같은 정적 호스팅만으로는 `/api/aim` 좌표 중계가 동작하지 않습니다.
